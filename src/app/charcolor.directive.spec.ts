import { CharcolorDirective } from './charcolor.directive';

describe('CharcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new CharcolorDirective();
    expect(directive).toBeTruthy();
  });
});
